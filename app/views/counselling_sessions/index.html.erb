<h2 class="page_title">Sessions</h1>
<%= datagrid_table @grid, @assets %>
<div class="digg_pagination">
  <div class="page_info">
    <%= page_entries_info @assets %>
  </div>
  <%= will_paginate @assets, :container => false %>
</div>
<%= button_to "New", new_resource_url, :method => "get" %>
